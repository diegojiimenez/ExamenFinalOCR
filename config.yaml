# Configuración del Sistema OCR

model:
  path: "models/trained_model.h5"
  input_shape: [32, 32, 1]  # 🔄 CAMBIADO: 28x28 → 32x32
  char_map: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789"  # 62 caracteres

preprocessing:
  blur_kernel: 5
  adaptive_thresh_block: 11
  adaptive_thresh_c: 2
  
segmentation:
  min_area: 50
  min_width: 5
  min_height: 10

training:
  epochs: 50
  batch_size: 32
  learning_rate: 0.001
  test_split: 0.2

paths:
  data_root: "data/"
  dataset: "data/dataset/"
  referencia: "data/referencia/"
  pruebas: "data/pruebas/"
  test_images: "data/pruebas/"
  output: "output/"
  
  dataset_ia: "data/dataset/DATASET_IA/"
  dataset_completo: "data/dataset/datasetCompleto/"
  dataset_completo2: "data/dataset/DatasetCompleto2/"
  
  mayusculas: "data/referencia/Mayúsculas/"
  minusculas: "data/referencia/Minúsculas/"
  numeros: "data/referencia/Números/"